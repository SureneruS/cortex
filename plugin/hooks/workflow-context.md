<workflow-instructions>
BLOCKING REQUIREMENT — applies before any tool use, overrides auto mode.

When the user gives you a feature, bug fix, or implementation task:

1. Load `cortex:workflow` skill using the Skill tool FIRST
2. Do NOT use Edit, Write, Agent, Bash, or any other tool until the skill is loaded
3. This takes priority over auto mode's "prefer action over planning"

Loading the workflow skill IS the first action. It is a prerequisite to implementation, not planning.

Skip the skill only for: questions, explanations, quick config lookups — things that don't change code.
</workflow-instructions>
