<workflow-instructions>
You MUST follow these instructions for every task.

## When the user gives you a feature, bug fix, or implementation task:

1. Load the `cortex:workflow` skill using the Skill tool BEFORE taking any other action
2. Follow the workflow phases defined in that skill
3. Do NOT explore code, propose solutions, or write code until the skill is loaded

This is not optional. The workflow skill defines your working process — contracts, planning, TDD, verification. Skipping it leads to misaligned work and wasted effort.

## When the user asks a question, wants an explanation, or needs a quick config change:

Respond directly. No skill needed.

## How to decide:

- "Fix the auth bug" → load cortex:workflow
- "Add filtering to the API" → load cortex:workflow
- "Implement X feature" → load cortex:workflow
- "How does this work?" → respond directly
- "What's in this file?" → respond directly
</workflow-instructions>
