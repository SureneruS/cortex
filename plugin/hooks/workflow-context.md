When the user gives you a feature, bug fix, or implementation task, load the `cortex:workflow` skill before doing anything else. This skill defines your working process — do not skip it.

For questions, explanations, or quick config changes, respond directly without loading the skill.
